package com.baidu.tcpdump;import android.content.BroadcastReceiver;import android.content.Context;import android.content.Intent;import android.util.Log;public class ShutdownReceiver extends BroadcastReceiver {     public void onReceive(Context context, Intent intent) {                 if(intent.getAction().equals(Intent.ACTION_SHUTDOWN)){        	              System.out.println("----------******----------");             System.out.println("Shut down this system!!!");             Log.i("MainActivity", "Shut down this system!!!");                          Intent activity = new Intent();             activity.setClass(context, MainActivity.class);             activity.putExtra("cmd", "stop_tcpdump");             //calling startActivity() from outside of an Activity context requires the {@link #Intent.FLAG_ACTIVITY_NEW_TASK} flag.             activity.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);             context.startActivity(activity);         }     } }